#include "viewport.h"

namespace ftdgl {
namespace viewport {
// double viewport_s::FontSizeToViewport(const FontPtr & font, double v, bool horz) const {
//     float pt_width = pixel_width * 72 / dpi;
//     float pt_height = pixel_height * 72 / dpi_height;

//     if (horz) {
//         return (v) * window_width * font->GetPtSize() / pt_width / 2;
//     } else {
//         return (v) * window_height * font->GetPtSize() / pt_height / 2;
//     }
// }

} //namespace viewport
} //namespace ftdgl
